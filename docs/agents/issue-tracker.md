# Issue tracker

Komodash uses **GitHub Issues** at https://github.com/dazu5/komodash/issues as its single source of truth for work tracking. PRDs, vertical-slice issues, bugs, and triage all live there.

The `gh` CLI is the supported way for agents to interact with the tracker. Agents should authenticate as `dazu5`.
